# Default Linux configuration.

alias dir='ls -l --color=auto --group-directories-first'
alias ll='ls -lh --color=auto --group-directories-first'
alias la='ls -alh --color=auto --group-directories-first'

# Aliases specific to Linux.
alias am='alsamixer -c 1'
